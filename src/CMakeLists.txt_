FILE(GLOB_RECURSE app_sources *.cpp)

idf_component_register( SRCS ${app_sources} 
                    INCLUDE_DIRS ../include
                    PRIV_REQUIRES esp_app_format vfs nvs_flash json dht ds18x20 spiffs esp_adc 
                    )

                    #                    PRIV_REQUIRES esp_app_format vfs esp32_wifi_manager nvs_flash json dht ds18x20 spiffs esp_adc 
